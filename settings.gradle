rootProject.name = 'CommentTest'

plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "compf_DocTest"
    property "sonar.organization", "compf"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
